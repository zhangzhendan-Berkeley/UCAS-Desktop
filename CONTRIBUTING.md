# 参与维护

## 报告问题

请包含 Windows/Python/Node/Edge 版本、本项目提交号、模块名、操作步骤、期望结果和脱敏日志。学校页面变动时可提供不含账号信息的页面结构描述。

不要上传账号密码、Token、Cookie、二维码原图、私人课程链接、`data/` 或完整浏览器资料。若涉及凭据泄露，先撤销/更换对应凭据，不要在公开 Issue 粘贴秘密。

## 提交修改

1. Fork 并创建分支，先按 README 安装运行环境。
2. 将平台差异放在对应适配器，避免让界面直接处理学校认证。
3. 新来源要登记在 `modules.json` 与 `THIRD_PARTY.md`，注明上游版本及许可；不要直接提交 `vendor/`。
4. 对认证、重复提交、结果判断、任务生命周期的更改添加有意义的离线测试；不在 CI 使用真实学校账号。
5. 保留“结果未知”的状态，不把页面出现泛泛的“成功”当成目标操作成功。
6. 更新使用说明与验证边界，运行受影响测试后提交 PR。

你的原创贡献按本仓库 AGPL-3.0-only 提交。第三方内容必须保留来源和原始许可；如果许可不明确，先提供链接和说明，不要将整个项目拷贝到 PR。
